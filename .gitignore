# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
# These are generated during build and should never be committed
# Docker builds these automatically, committing them causes merge conflicts
dist/
build/
.output/
.vercel/
.netlify/

# SvelteKit
# .svelte-kit/ contains generated files that change on every build
# web/build/ is the static output directory created by adapter-static
# These are rebuilt by Docker on deployment
web/.svelte-kit/
web/build/
web/node_modules/

# Environment
# Never commit secrets or local environment configuration
.env
.env.local
.env.*.local
web/.env

# OS
# macOS and Windows system files
.DS_Store
**/.DS_Store
Thumbs.db
._*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# SQLite
# Database files are managed by Docker volumes
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db

# Docker
docker-compose.override.yml

# Logs
logs/
*.log

# --- Web build output / SvelteKit ---
web/node_modules/
web/.svelte-kit/
web/build/
web/.vite/
web/.DS_Store
web/**/.DS_Store

# --- API ---
api/node_modules/
api/dist/

# --- macOS AppleDouble junk ---
**/._*

# build artefacts
.svelte-kit/
build/
node_modules/
